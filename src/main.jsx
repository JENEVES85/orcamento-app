import React from 'react';
import ReactDOM from 'react-dom/client';
import OrcamentoApp from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <OrcamentoApp />
  </React.StrictMode>
);
